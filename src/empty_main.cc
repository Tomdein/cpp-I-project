#include <fstream>

int main(int, char *[])
{
    // std::ofstream f("../../src/dummy.cc", std::ios_base::out);

    // f << "{";

    // for (int i = 0; i < 256; i++)
    // {
    //     f << "{" << i << ", " << i << ", " << i << "},\n";
    // }

    // f << "}";
}