#include <iostream>
#include <fstream>

#include "parser.h"

int main(int, char *[])
{

    // std::cout << sizeof(paint::grayscale_palette) << std::endl;

    // std::ofstream f("../../src/dummy.cc", std::ios_base::out);

    // f << "{";

    // for (int i = 0; i < 256; i++)
    // {
    //     f << "{" << i << ", " << i << ", " << i << "},\n";
    // }

    // f << "}";
}